/**
 * Default Macro Definitions
 *
 * Macros have been moved to workflow JSON files in the /flows folder at the repo root.
 * Users can import these workflows manually if needed.
 * This file now exports an empty array as macros are user-defined.
 */

import type { Flow } from 'zipp-core';

export const defaultMacros: Flow[] = [];
