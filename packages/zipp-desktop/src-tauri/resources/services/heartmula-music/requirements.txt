# HeartMuLa Music Generation Server Requirements

# Web framework
fastapi>=0.100.0
uvicorn>=0.23.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Audio
soundfile>=0.12.0

# ML
safetensors>=0.4.0
transformers>=4.40.0
tokenizers>=0.19.0
einops>=0.8.0
accelerate>=1.0.0
huggingface_hub>=0.20.0

# HeartMuLa dependencies (heartlib installed via start.bat with --no-deps)
vector-quantize-pytorch>=1.20.0

# The following are installed separately by start.bat with CUDA support:
# - torch, torchvision, torchaudio (from pytorch index with CUDA)
# - heartlib (from git with --no-deps to avoid PyTorch downgrade)
# - torchao (for quantization support)
